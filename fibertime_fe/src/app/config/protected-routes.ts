// List all protected routes here. Add additional routes as needed.
// This is used to check if the user is authenticated before accessing these routes.
export const protectedRoutes = [
    '/bundles',
];